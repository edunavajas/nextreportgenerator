apply plugin: "war"

bootWar {
    mainClass = "com.healthincode.nextreportgenerator.NextreportgeneratorApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
